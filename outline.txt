1: take a command entered by user

2: divide that command up into tokens (strtok or strtok_r) divided by spaces

3: place the tokens into the CommandLine struct 
    a. command (cat, status, sleep, ls)
    b. arguments (-al, -f)
        1. array
        2. use calloc for each string token inserted in the array
    c. input_file (< file_name)
    d. output_fle (> file_name)
    e. background or foreground? (&)